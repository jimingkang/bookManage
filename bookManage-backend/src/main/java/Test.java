/*
 * package com.example.demo;
 * 
 * import java.util.*;
 * 
 * public class Test {
 * 
 * public static void main(String[] args) { // TODO Auto-generated method stub
 * 
 * String[] a=new String[] {"cat","tca","dog","god","est"}; //0 ->cat,tac //
 * 1->dog , }
 * 
 * public static Map catgory(String[] animals) { Map<Integer,List<String>> a=new
 * HashMap<Integer,List<String>>(); for(int i=0;i<a.size();i++) { //List li=new
 * ArrayList()； // li.add(e); if(i==0) { // a.put(i, animals[i]); }
 * 
 * else if(1==1) {
 * 
 * }
 * 
 * 
 * } return a; }
 * 
 * public static boolean ifsimilar(String str,List li) { char[]
 * ch=str.toCharArray(); char[] ch2=str.toCharArray();
 * 
 * if(str.length()==str2.length()) { int i=0; for(;i<ch.length;i++)
 * if(str.contains(ch[i])) {str=str.substring(0,
 * str.indexOf(ch[i]))+str.substring( str.indexOf(ch[i])+1,str.length());
 * str2=str2.substring(0, str2.indexOf(ch[i]))+str2.substring(
 * str2.indexOf(ch[i])+1,str2.length());
 * 
 * 
 * 
 * } if(i==ch.length) return true;
 * 
 * } return false; } public static int findMax(int[] arr,int k) { int ret=0; int
 * tmp=0; for(int i=0;i<arr.length;i++) { //int one_time_max=0; for(int
 * j=i+1;j<arr.length;j++) { tmp=arr[j]-arr[i]; if(ret<tmp) { ret=max(arr,j); }
 * } } return ret; } public static int max(int[] arr,int i) { int max=0;
 * 
 * for(int i=0;i<arr.length;i++) { if(max<arr[i]) { ret=tmp; } }
 * 
 * return max; }
 * 
 * 
 * }
 */